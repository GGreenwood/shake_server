res.write(string)   write to the response
res.end()     push the response
res.send(msp)   writes and pushes
res.status()    gets status

db

USERS

id          LONG
fb_id       VARCHAR
fb_token    VARCHAR
email       VARCHAR

